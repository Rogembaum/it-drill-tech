/* ============================================================================
   ENGLISH TRANSLATIONS — QUESTIONS
   Keyed by the "key" field of each question in data/questions.js
   (the key looks like "linux.io-cache.sen").

   Anything missing falls back to the Russian original and the item is
   marked RU in the interface, so this file can be filled in a little at a time.

   Shape of one entry — every field is optional:

     "linux.perm.jun": {
       q:    "What does -rw-r--r-- mean in ls -l output?",
       a:    "The short answer you would say out loud.",
       d:    ["Detail one.", "Detail two."],
       code: `ls -l /etc/passwd`,
       f:    ["Follow-up question?"]
     },

   Check coverage with:  node tools/validate.mjs
   ============================================================================ */
window.QUESTIONS_EN = {
};
